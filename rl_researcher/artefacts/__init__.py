"""Writers for the documents a run leaves behind.

Each module here turns a finished run's files into one artefact kind: a report, a live
dashboard, a measurement, a diagnosis, the state page, an index. They share the block
inventory and one stylesheet, so every instance of a kind has the same shape.
"""
